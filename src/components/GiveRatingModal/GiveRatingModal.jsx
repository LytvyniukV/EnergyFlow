import Modal from "../../shared/components/Modal/Modal";
import css from "./GiveRatingModal.module.css";
export default function GiveRatingModal({ isModal, onClose }) {
  <Modal isModal={isModal} onClose={onClose}>
    <p>Heloo</p>
  </Modal>;
}
